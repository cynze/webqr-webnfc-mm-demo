import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'zeya-selected-topics',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
